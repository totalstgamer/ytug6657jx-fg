### Civil Wars

chinese_civil_war = {
	OR = {
		PRC = {
			has_war_with = MAN
			has_war_with = SHX
			has_war_with = REG
		}
		MAN = {
			OR = {
				has_war_with = PRC
				has_war_with = SHX
				has_war_with = REG
			}
		}
		SHX = {
			OR = {
				has_war_with = PRC
				has_war_with = MAN
				has_war_with = REG
			}
		}
	}
}